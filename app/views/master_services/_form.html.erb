<%= form_with(model: master_service) do |form| %>
  <% if master_service.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(master_service.errors.count, "error") %> prohibited this master_service from being saved:</h2>

      <ul>
        <% master_service.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :service %>
    <%= form.text_field :service %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
